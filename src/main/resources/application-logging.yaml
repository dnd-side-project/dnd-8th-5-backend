spring:
  config:
    activate:
      on-profile: local
logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
      springframework:
        security: DEBUG
        oauth2: TRACE

---
spring:
  config:
    activate:
      on-profile: prod
