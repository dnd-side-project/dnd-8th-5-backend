spring:
  profiles:
    active: local
    include:
      - db
      - oauth2
      - host
      - security

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
